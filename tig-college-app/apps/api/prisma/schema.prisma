model StudentNote {
  id        String        @id @default(cuid())
  title     String
  content   String
  createdAt DateTime      @default(now())

 
  student   StudentMaster @relation(fields: [studentId], references: [id])
  studentId String
}
